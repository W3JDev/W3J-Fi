{
  "version": 2,
  "name": "w3jdev-united",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.php"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "VITE_API_URL": "@api_url",
    "VITE_BASE_URL": "@base_url"
  },
  "build": {
    "env": {
      "VITE_API_URL": "@api_url",
      "VITE_BASE_URL": "@base_url"
    }
  }
}
